
<div class="flex justify-between items-start">
  <h2 class="text-2xl font-bold"><%= training_plan.resource.name %></h2>
  <img src="<%= training_plan.resource.image_url %>" alt="<%= training_plan.resource.name %>" class="w-32 h-32 object-cover">
</div>
<p><strong>Difficulté:</strong> <%= training_plan.resource.difficulty %></p>
<p><strong>Prix:</strong> <%= training_plan.resource.price %></p>
<div class="bg-gray-100 p-4 rounded">
  <p><%= training_plan.resource.content %></p>
</div>
<p><a href="<%= training_plan.resource.resource_url %>" target="_blank" class="text-blue-500 underline">Voir la ressource</a></p>
<button id="complete-button" class="mt-4 px-4 py-2 bg-blue-500 text-white rounded">Fait!</button>
